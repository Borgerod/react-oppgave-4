// "use client";
// import { createTheme } from "@mui/material/styles";

// // Minimal MUI theme: do NOT set a palette so MUI doesn't force colors.
// // Your app should drive colors via CSS variables (in `globals.css`) and Tailwind.
// const theme = createTheme({
// 	typography: {
// 		fontFamily: "var(--font-sans, var(--font-roboto, Arial))",
// 	},
// 	colorSchemes: {
// 		dark: true,
// 	},
// });
// export default theme;
